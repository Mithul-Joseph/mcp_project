[project]
name = "mcp-git"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.9.1",
    "nest-asyncio>=1.6.0",
    "ollama>=0.4.8",
]
